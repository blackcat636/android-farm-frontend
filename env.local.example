# API Configuration
# Базовий URL API агента (використовується як fallback)
NEXT_PUBLIC_API_URL=http://localhost:3000

# Використовувати тунель для отримання URL
# Якщо true - фронтенд буде намагатися отримати URL з Cloudflare KV
# Якщо false - використовується NEXT_PUBLIC_API_URL
NEXT_PUBLIC_USE_TUNNEL=true

# Cloudflare KV Configuration (для API routes - server-side only)
# Ці змінні використовуються тільки на сервері (API routes), не доступні в браузері
# Для Cloudflare Pages: додайте ці змінні в Environment Variables на Cloudflare Dashboard
# Для локальної розробки: додайте в .env.local (без NEXT_PUBLIC_ префіксу)
CLOUDFLARE_ACCOUNT_ID=your-account-id-here
CLOUDFLARE_API_TOKEN=your-api-token-here
CLOUDFLARE_KV_NAMESPACE_ID=your-kv-namespace-id-here

# Примітка:
# - Змінні з префіксом NEXT_PUBLIC_ доступні в браузері
# - Змінні без NEXT_PUBLIC_ доступні тільки на сервері (безпечно для credentials)
# - Скопіюйте цей файл як .env.local і заповніть значення
# - .env.local не повинен бути в git (вже додано в .gitignore)
# - Для Cloudflare Pages: додайте CLOUDFLARE_* змінні в Environment Variables на Dashboard
# - Детальні інструкції дивіться в CLOUDFLARE_KV_FRONTEND.md


