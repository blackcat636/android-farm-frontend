# Детальний аналіз проекту Android Farm Frontend

## 📋 Загальний опис

**Android Farm Frontend** - це Next.js веб-додаток для управління агентами, виконання автоматизованих дій на Android емуляторах, управління соціальними аккаунтами, чергою завдань та перегляду історії виконання. Фронтенд інтегрується з бекендом через REST API та підтримує автентифікацію через Supabase Auth.

---

## 🏗️ Архітектура проекту

### Технологічний стек

- **Next.js 16** - React фреймворк з App Router
- **TypeScript** - мова програмування
- **React 19** - UI бібліотека
- **Ant Design 6** - UI компоненти
- **Axios** - HTTP клієнт
- **Tailwind CSS 4** - стилізація
- **Cloudflare KV** - опціональна інтеграція для отримання агентів

### Структура проекту

```
frontend/
├── app/                    # Next.js App Router сторінки
│   ├── (public)/          # Публічні роути (login, register)
│   ├── accounts/          # Управління соціальними аккаунтами
│   ├── emulators/         # Список емуляторів
│   ├── history/           # Історія виконання
│   ├── platforms/         # Платформи та дії
│   ├── queue/             # Черга завдань
│   ├── layout.tsx         # Головний layout
│   └── page.tsx           # Dashboard
├── components/            # React компоненти
│   ├── accounts/          # Компоненти для аккаунтів
│   ├── Agents/            # Компоненти для агентів
│   ├── auth/              # Компоненти автентифікації
│   ├── common/            # Загальні компоненти
│   └── Layout/            # Layout компоненти
├── contexts/              # React Context провайдери
│   ├── AgentsContext.tsx  # Контекст агентів
│   └── AuthContext.tsx    # Контекст автентифікації
├── hooks/                 # Custom React hooks
├── lib/                   # Бібліотеки та утиліти
│   └── api/               # API клієнти
├── types/                 # TypeScript типи
└── utils/                 # Утиліти
```

---

## 🔧 Основні можливості

### 1. Автентифікація

#### Функціонал
- **Sign In** - вхід через email/password
- **Sign Up** - реєстрація нового користувача
- **Sign Out** - вихід з системи
- **Token Management** - автоматичне зберігання та оновлення токенів
- **Auto Refresh** - автоматичне оновлення access token при 401 помилці

#### Зберігання токенів
- **localStorage** - зберігання `auth_token` та `refresh_token`
- **Автоматичне додавання** токену до всіх запитів
- **Interceptor** - автоматичне оновлення токену при 401

**Файли:**
- `contexts/AuthContext.tsx` - контекст автентифікації
- `lib/api/backend.ts` - API клієнт з токен менеджментом
- `components/auth/ProtectedRoute.tsx` - захист роутів

---

### 2. Управління агентами

#### Функціонал
- **Додавання агентів** - ручне додавання з тестуванням підключення
- **Вибір активного агента** - перемикання між агентами
- **Оновлення URL тунелю** - автоматичне та ручне оновлення
- **Синхронізація з Cloudflare KV** - автоматичне завантаження агентів
- **Зберігання в localStorage** - кешування агентів локально

#### Джерела агентів
1. **Cloudflare KV** (пріоритет, якщо `NEXT_PUBLIC_USE_TUNNEL=true`)
2. **localStorage** (fallback)
3. **Дефолтний агент** (якщо немає агентів)

#### Особливості
- **Автоматичне завантаження** tunnelUrl для активного агента
- **Тестування підключення** при додаванні нового агента
- **Відображення статусу** - індикатор наявності тунелю (🌐)

**Файли:**
- `contexts/AgentsContext.tsx` - контекст агентів
- `components/Agents/AgentSelector.tsx` - селектор агентів
- `lib/api/cloudflare-kv.ts` - клієнт для Cloudflare KV

---

### 3. Dashboard

#### Функціонал
- **Статус агента** - перевірка доступності через healthcheck
- **Статистика** - кількість платформ, емуляторів, активних емуляторів
- **Список платформ** - відображення доступних платформ
- **Список емуляторів** - відображення всіх емуляторів з усіх агентів

#### Особливості
- **Автоматичне оновлення** при зміні активного агента
- **Обробка помилок** - детальні повідомлення про проблеми підключення
- **Візуальні індикатори** - статуси, теги, іконки

**Файли:**
- `app/page.tsx` - головна сторінка Dashboard

---

### 4. Платформи та дії

#### Функціонал
- **Список платформ** - автоматичне отримання з агента
- **Список дій** - для кожної платформи
- **Виконання дій** - через динамічні або спеціалізовані форми
- **Спеціалізовані сторінки** - для конкретних дій (youtube/search, instagram/post)

#### Структура сторінок
- `/platforms` - список всіх платформ
- `/platforms/[platform]` - деталі платформи
- `/platforms/[platform]/[action]` - динамічна форма для дії
- `/platforms/youtube/search` - спеціалізована форма для YouTube search
- `/platforms/instagram/post` - спеціалізована форма для Instagram post
- `/platforms/instagram/login` - спеціалізована форма для Instagram login

#### Особливості
- **Динамічне завантаження** - платформи та дії отримуються з API
- **Вибір емулятора** - для кожної дії
- **JSON параметри** - для загальних форм
- **Спеціалізовані форми** - для конкретних дій з валідацією

**Файли:**
- `app/platforms/page.tsx` - список платформ
- `app/platforms/[platform]/[action]/page.tsx` - динамічна форма
- `app/platforms/youtube/search/page.tsx` - спеціалізована форма
- `app/platforms/instagram/post/page.tsx` - спеціалізована форма

---

### 5. Емулятори

#### Функціонал
- **Список емуляторів** - з усіх агентів
- **Фільтрація** - по статусу (active/inactive)
- **Відображення метаданих** - назва, UDID, статус, агент
- **Інтеграція з Dashboard** - відображення статистики

#### Особливості
- **Агрегація з усіх агентів** - паралельне завантаження
- **Обробка помилок** - якщо агент недоступний, показуємо інші
- **Візуальні індикатори** - теги для статусів та агентів

**Файли:**
- `app/emulators/page.tsx` - сторінка емуляторів
- `hooks/useAllEmulators.ts` - хук для отримання емуляторів

---

### 6. Історія виконання

#### Функціонал
- **Список викликів** - з фільтрацією та пагінацією
- **Статистика** - total, success, error, pending
- **Фільтри** - по статусу, платформі
- **Деталі запису** - платформа, дія, емулятор, результат, помилки

#### Особливості
- **Фільтрація по користувачу** - автоматично по поточному користувачу
- **Візуальні індикатори** - кольорові теги для статусів
- **Форматування дат** - локалізований формат (uk-UA)
- **Відображення тривалості** - в секундах

**Файли:**
- `app/history/page.tsx` - сторінка історії

---

### 7. Черга завдань

#### Функціонал
- **Список завдань** - з фільтрацією та пагінацією
- **Статистика** - total, pending, processing, completed, failed
- **Фільтри** - по статусу, платформі
- **Скасування завдань** - для pending/assigned/processing статусів
- **Деталі завдання** - розгортання рядка для деталей

#### Статуси завдань
- `pending` - очікує (orange)
- `assigned` - призначено (blue)
- `processing` - виконується (cyan)
- `completed` - завершено (green)
- `failed` - помилка (red)
- `cancelled` - скасовано (default)

#### Особливості
- **Автоматичне оновлення** - кнопка оновлення
- **Розгортання рядків** - детальна інформація про завдання
- **Відображення аккаунтів** - ID аккаунту (якщо вказано)
- **Відображення емуляторів** - призначений або тип емулятора

**Файли:**
- `app/queue/page.tsx` - сторінка черги

---

### 8. Соціальні аккаунти

#### Функціонал
- **Список аккаунтів** - з фільтрацією та пагінацією
- **Створення аккаунту** - через модальне вікно
- **Редагування аккаунту** - через модальне вікно
- **Видалення аккаунту** - з підтвердженням
- **Деталі аккаунту** - через Drawer (проксі, прив'язки, статистика)
- **Статистика** - total, active, banned, withProxy

#### Фільтри
- **Платформа** - instagram, youtube, tiktok, facebook, twitter
- **Статус** - active, banned, restricted, suspended, inactive
- **Проксі** - потрібен/не потрібен

#### Особливості
- **Шифрування паролів** - паролі не відображаються, тільки при редагуванні
- **Відображення статистики** - total_tasks, successful_tasks, failed_tasks
- **Візуальні індикатори** - кольорові теги для статусів
- **Детальна інформація** - Drawer з повною інформацією про аккаунт

**Файли:**
- `app/accounts/page.tsx` - сторінка аккаунтів
- `components/accounts/CreateAccountModal.tsx` - модальне вікно створення
- `components/accounts/EditAccountModal.tsx` - модальне вікно редагування
- `components/accounts/AccountDetailsDrawer.tsx` - Drawer з деталями

---

### 9. Layout та навігація

#### Компоненти Layout
- **AppLayout** - головний layout з сайдбаром
- **AppHeader** - заголовок з селектором агентів та меню користувача
- **Sider** - бічне меню навігації
- **Content** - область контенту

#### Навігація
- **Dashboard** (`/`) - головна сторінка
- **Platforms** (`/platforms`) - платформи
- **Emulators** (`/emulators`) - емулятори
- **Аккаунти** (`/accounts`) - соціальні аккаунти
- **Черга** (`/queue`) - черга завдань
- **Історія** (`/history`) - історія виконання

#### Особливості
- **Захист роутів** - ProtectedRoute для захищених сторінок
- **Публічні роути** - `/login`, `/register` без layout
- **Активний роут** - підсвічування в меню
- **Collapsible Sider** - згортання бічного меню

**Файли:**
- `components/Layout/AppLayout.tsx` - головний layout
- `components/Layout/AppHeader.tsx` - заголовок
- `components/auth/ProtectedRoute.tsx` - захист роутів

---

## 🌐 API інтеграція

### Backend API Client

#### Автоматичне управління токенами
- **Request Interceptor** - автоматичне додавання токену
- **Response Interceptor** - автоматичне оновлення токену при 401
- **Queue для refresh** - уникнення паралельних refresh запитів

#### Методи API
- **Auth** - signIn, signUp, signOut, getMe, refresh
- **Agents** - getAgents, getAgent
- **Proxy** - getHealth, getPlatforms, getPlatformActions, getEmulators, executeAction
- **History** - getHistory, getHistoryRecord, getHistoryStats
- **Queue** - getQueue, getTask, addTask, cancelTask
- **Social Accounts** - createSocialAccount, getSocialAccounts, getSocialAccount, updateSocialAccount, deleteSocialAccount
- **Account Proxies** - createProxy, getProxyForAccount, updateProxy, checkProxyHealth
- **Account Bindings** - createBinding, getBindingForAccount, getBindingsForEmulator, updateBinding
- **Sync** - syncAgents

**Файли:**
- `lib/api/backend.ts` - головний API клієнт

---

### Custom Hooks

#### useBackendAgentApi
- **Отримання API клієнта** для активного агента
- **Перевірка підключення** - isConnected
- **Автоматичне оновлення** при зміні активного агента

#### useAllEmulators
- **Отримання емуляторів** з усіх агентів
- **Фільтрація** - тільки активні або всі
- **Агрегація** - об'єднання емуляторів з різних агентів

#### useAgentApi
- **Виконання дій** через бекенд
- **Обробка помилок** - автоматичне відображення
- **Loading state** - стан завантаження

**Файли:**
- `hooks/useBackendAgentApi.ts`
- `hooks/useAllEmulators.ts`
- `hooks/useAgentApi.ts`

---

## ⚙️ Конфігурація

### Змінні середовища (`.env.local`)

```env
# Backend API URL
NEXT_PUBLIC_BACKEND_URL=http://localhost:4000

# Use tunnel mode for agents (optional)
NEXT_PUBLIC_USE_TUNNEL=true

# Cloudflare KV Configuration (for legacy support)
NEXT_PUBLIC_CLOUDFLARE_ACCOUNT_ID=
NEXT_PUBLIC_CLOUDFLARE_API_TOKEN=
NEXT_PUBLIC_CLOUDFLARE_KV_NAMESPACE_ID=
```

---

## 🔄 Життєвий цикл додатку

### Ініціалізація

1. **Завантаження AuthContext** - перевірка токену, отримання користувача
2. **Завантаження AgentsContext** - завантаження агентів з KV/localStorage
3. **Встановлення активного агента** - з localStorage або перший доступний
4. **Автоматичне оновлення tunnelUrl** - для активного агента

### Виконання дії

1. **Вибір платформи та дії** - на сторінці `/platforms`
2. **Заповнення форми** - параметри та вибір емулятора
3. **Відправка запиту** - через `useBackendAgentApi` → `backendClient.executeAction()`
4. **Проксування через бекенд** - бекенд проксує до агента
5. **Створення запису в історії** - автоматично бекендом
6. **Оновлення історії** - після виконання (success/error)
7. **Відображення результату** - на сторінці виконання

---

## 📊 State Management

### React Context

#### AuthContext
- **user** - поточний користувач
- **loading** - стан завантаження
- **signIn** - функція входу
- **signUp** - функція реєстрації
- **signOut** - функція виходу
- **refreshUser** - оновлення користувача

#### AgentsContext
- **agents** - список агентів
- **activeAgent** - активний агент
- **addAgent** - додавання агента
- **updateAgent** - оновлення агента
- **deleteAgent** - видалення агента
- **setActiveAgent** - встановлення активного агента
- **refreshAgentTunnelUrl** - оновлення URL тунелю

### Local Storage

#### Зберігання даних
- **auth_token** - access token
- **refresh_token** - refresh token
- **android-farm-agents** - список агентів
- **android-farm-active-agent-id** - ID активного агента

---

## 🎨 UI/UX

### Ant Design Components

#### Використовувані компоненти
- **Layout** - AppLayout, Header, Sider, Content
- **Navigation** - Menu
- **Forms** - Form, Input, Select, Button, Switch
- **Data Display** - Table, Card, List, Tag, Statistic, Descriptions
- **Feedback** - Modal, Drawer, Alert, Message, Result, Loading
- **Data Entry** - DatePicker, Input.TextArea

#### Стилізація
- **Tailwind CSS 4** - для кастомних стилів
- **Ant Design Theme** - стандартна тема Ant Design
- **Responsive Design** - адаптивний дизайн

---

## 🛡️ Безпека

### Автентифікація
- **JWT токени** - зберігання в localStorage
- **Автоматичне оновлення** - refresh token при 401
- **Захист роутів** - ProtectedRoute компонент
- **Перевірка токену** - на кожному запиті

### Обробка помилок
- **Try-catch блоки** - обробка помилок в усіх async функціях
- **Відображення помилок** - через Alert та Message компоненти
- **Fallback значення** - для недоступних агентів

---

## 🚀 Запуск та розгортання

### Розробка
```bash
npm install
npm run dev
```

Відкрийте [http://localhost:3001](http://localhost:3001) в браузері.

### Продакшн
```bash
npm run build
npm start
```

### Збірка
```bash
npm run build
```

---

## 📝 Структура сторінок

### Публічні роути
- `/login` - сторінка входу
- `/register` - сторінка реєстрації

### Захищені роути
- `/` - Dashboard
- `/platforms` - Список платформ
- `/platforms/[platform]` - Деталі платформи
- `/platforms/[platform]/[action]` - Виконання дії
- `/platforms/youtube/search` - YouTube search
- `/platforms/instagram/post` - Instagram post
- `/platforms/instagram/login` - Instagram login
- `/emulators` - Список емуляторів
- `/accounts` - Соціальні аккаунти
- `/queue` - Черга завдань
- `/history` - Історія виконання

---

## 🔮 Розширення функціоналу

### Додавання нової платформи
Платформи визначаються на агенті, фронтенд автоматично підхопить їх через API.

### Додавання спеціалізованої форми
1. Створити файл `app/platforms/[platform]/[action]/page.tsx`
2. Або створити конкретну сторінку, наприклад `app/platforms/myplatform/myaction/page.tsx`
3. Використати `useBackendAgentApi` для виконання дії

### Додавання нового роуту
1. Створити файл в `app/` директорії
2. Додати пункт меню в `AppLayout.tsx`
3. Додати захист через `ProtectedRoute` (якщо потрібно)

---

## 📚 Ключові компоненти

### Common Components

#### Loading
- **Відображення завантаження** - спінер або skeleton

#### ErrorDisplay
- **Відображення помилок** - Alert з повідомленням та описом

### Layout Components

#### AppLayout
- **Головний layout** - з сайдбаром та контентом
- **Навігація** - меню з роутами
- **Захист** - ProtectedRoute для захищених роутів

#### AppHeader
- **Селектор агентів** - AgentSelector
- **Меню користувача** - Dropdown з опціями
- **Синхронізація агентів** - кнопка синхронізації з KV

### Account Components

#### CreateAccountModal
- **Створення аккаунту** - форма з валідацією

#### EditAccountModal
- **Редагування аккаунту** - форма з попередньо заповненими даними

#### AccountDetailsDrawer
- **Деталі аккаунту** - Drawer з повною інформацією
- **Проксі** - відображення та управління проксі
- **Прив'язки** - відображення прив'язок до емуляторів
- **Статистика** - детальна статистика аккаунту

---

## 🎯 Ключові переваги архітектури

1. **Модульність** - чітке розділення компонентів, хуків, контекстів
2. **Типізація** - повна TypeScript підтримка
3. **Переіспользованість** - custom hooks та компоненти
4. **Автоматизація** - автоматичне управління токенами, оновлення даних
5. **UX** - детальна обробка помилок, loading states, візуальні індикатори
6. **Масштабованість** - підтримка багатьох агентів та емуляторів
7. **Гнучкість** - динамічні форми та спеціалізовані сторінки

---

## 📝 Важливі примітки

### Next.js App Router
- **Server Components** - за замовчуванням
- **Client Components** - з `'use client'` директивою
- **Edge Runtime** - для деяких сторінок (опціонально)

### Cloudflare KV
- **Опціонально** - фронтенд працює без KV
- **Автоматичне завантаження** - якщо `NEXT_PUBLIC_USE_TUNNEL=true`
- **Fallback** - localStorage якщо KV недоступний

### Token Management
- **Автоматичне оновлення** - через interceptor
- **Queue для refresh** - уникнення паралельних запитів
- **Безпека** - токени в localStorage (можна перейти на httpOnly cookies)

### Агенти
- **Пріоритет URL** - tunnel_url > url
- **Автоматичне оновлення** - tunnelUrl для активного агента
- **Синхронізація** - з Cloudflare KV або localStorage

---

## 🔍 Діагностика проблем

### Помилки підключення
- Перевірте `NEXT_PUBLIC_BACKEND_URL`
- Перевірте доступність бекенду
- Перевірте CORS налаштування

### Помилки автентифікації
- Перевірте токени в localStorage
- Перевірте валідність токенів
- Спробуйте вийти та увійти знову

### Помилки агентів
- Перевірте URL агента
- Перевірте доступність агента
- Перевірте налаштування тунелю

### Помилки завантаження даних
- Перевірте консоль браузера
- Перевірте Network tab в DevTools
- Перевірте логи бекенду

---

**Останнє оновлення:** 2024
**Версія фронтенду:** 0.1.0
